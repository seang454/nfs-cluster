# Managed by Ansible - DO NOT EDIT MANUALLY
127.0.0.1   localhost


# Ceph / NFS Storage Nodes (Using Internal VPC IPs)
{% for host in groups['ceph_nodes'] %}
{{ hostvars[host]['ip'] | default(hostvars[host]['ansible_host']) }}   {{ host }}
{% endfor %}

# Kubernetes / NFS Client Nodes (Using Internal VPC IPs)
{% for host in groups['nfs_clients'] %}
{{ hostvars[host]['ip'] | default(hostvars[host]['ansible_host']) }}   {{ host }}
{% endfor %}
