# Kubespray HA inventory - auto-generated by Terraform
# Topology: stacked etcd (same nodes as control plane)
# Do not edit manually. Terraform rewrites this file during apply.

[kube_control_plane]
%{ for node in control_plane_nodes ~}
${node.name} ansible_host=${node.public_ip} ip=${node.private_ip} etcd_member_name=${node.etcd_member_name}
%{ endfor }

[etcd:children]
kube_control_plane

[kube_node]
%{ for node in worker_nodes ~}
${node.name} ansible_host=${node.public_ip} ip=${node.private_ip}
%{ endfor }

[k8s_cluster:children]
kube_control_plane
kube_node

[calico_rr]

[all:vars]
ansible_user=${ansible_user}
ansible_ssh_private_key_file=${ansible_ssh_private_key_file}
ansible_python_interpreter=${ansible_python_interpreter}
ansible_ssh_extra_args='${ansible_ssh_extra_args}'
